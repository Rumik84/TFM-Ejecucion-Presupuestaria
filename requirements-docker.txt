# Dependencias para imágenes Docker (sin herramientas de desarrollo ni paquetes opcionales pesados).
# Genera imágenes ~60% más ligeras que requirements.txt completo.
#
# Excluidos deliberadamente:
#   great-expectations  → opcional en código (soft import), pesa ~2 GB de deps transitivas
#   pyaxis              → soft import con try/except en pcaxis_parser.py; PC-AXIS es formato raro
#   jupyterlab/ipykernel → herramientas de exploración, no de producción
#   pytest/ruff/black/mypy → CI/dev, no runtime

# ---------- Core ----------
python-dotenv>=1.0.0
pyyaml>=6.0
pydantic>=2.5
pydantic-settings>=2.1
typer>=0.9
rich>=13.7

# ---------- HTTP & Ingesta ----------
requests>=2.31
httpx>=0.26
tenacity>=8.2
aiohttp>=3.9
ratelimit>=2.2

# ---------- Data / ETL ----------
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
openpyxl>=3.1
xlrd>=2.0
lxml>=5.1
beautifulsoup4>=4.12
chardet>=5.2
python-dateutil>=2.8

# ---------- Validación de datos ----------
pandera>=0.18
jsonschema>=4.21

# ---------- Base de datos ----------
sqlalchemy>=2.0
sqlite-utils>=3.35

# ---------- Orquestación ----------
prefect>=2.16,<3.0
apscheduler>=3.10

# ---------- Modelado ----------
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.2
statsmodels>=0.14
joblib>=1.3
shap>=0.44

# ---------- Dashboard ----------
streamlit>=1.32
plotly>=5.19
altair>=5.2
streamlit-aggrid>=0.3

# ---------- Logging ----------
loguru>=0.7
